<div class="container">
  <div class="row">
    <div class="col-md-12">
      {{#power-calendar daysComponent="calendar-days" navComponent="calendar-nav" as |calendar|}}
        {{calendar.nav}}
        {{calendar.days}}
      {{/power-calendar}}
    </div>
  </div>
</div>
